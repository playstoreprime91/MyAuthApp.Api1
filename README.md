# CRUD App (Node.js + Express + HTML + Railway)

Prosty CRUD z interfejsem HTML i REST API, gotowy do deploymentu na Railway.

## Uruchomienie lokalnie
```bash
npm install
npx prisma migrate dev --name init
npm start
```

Wejdź na: [http://localhost:3000](http://localhost:3000)
